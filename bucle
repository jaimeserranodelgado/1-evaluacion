# BUCLES

def bucle_1():
    suma=0 # es una variable acumuladora
    for count in range(1,11):
        num=int(input("Dime un numero. "))
        suma=num+suma
        print(suma)
bucle_1()
        
